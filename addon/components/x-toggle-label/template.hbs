{{#if this.show}}
  <label
    for={{this.for}}
    class='toggle-text toggle-prefix {{this.labelType}}'
    {{on 'click' (action 'clickLabel')}}
    ...attributes
  > 
    {{#if hasBlock}}
      {{yield this.label}}
    {{else}}
      {{this.label}}
    {{/if}}
  </label>
{{/if}}
